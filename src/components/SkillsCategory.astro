---
import SkillsItem from "./SkillsItem.astro";


const { category, items } = Astro.props;

---
<div>
  <h3 class="text-2xl font-semibold mb-4">{category}</h3>
  <div class="flex flex-wrap gap-4">
      
      {items.map(({name, icon}) => (
        <SkillsItem name={name} icon={icon} />
      ))}
      
  </div>
<div>